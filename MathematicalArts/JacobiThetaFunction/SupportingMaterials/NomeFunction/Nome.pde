double Nome(double k_){
  
      double a, b, v;
      
      a = ellipk(k_);
      b = ellipk(k_);
      
      v = Math.exp(-Math.PI * a / b);
      
      return v;
}

double ellipk(double k_){
  
  
}
